---
title: "Untitled"
output: html_document
date: "2026-02-05"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:
```{r}
library(survival)
library(timereg)

# Przygotowanie danych
data(lung, package = "survival")

# 1. Czyszczenie danych: prop.odds nie lubi brakujących wartości (NA)
# Zbiór już zawiera zmienną sex2
lung_clean <- na.omit(lung[, c("time", "status", "age", "ph.ecog", "ph.karno", "sex")])


# 2. Dopasowanie modelu proporcjonalnych szans
outs <- prop.odds(Event(time, status ==2) ~ age + ph.ecog + ph.karno + sex,
                 data = lung_clean, n.sim = 0)
# Wyświetla tabelę współczynników dla age, ph.ecog, ph.karno i sex
print(outs$gamma)
```

## Including Plots

You can also embed plots, for example:
```{r}
library(survival)
library(timereg)

# Przygotowanie danych
lung_data <- survival::lung

# 1. Czyszczenie danych
lung_clean <- na.omit(lung[, c("time", "status", "age", "ph.ecog", "ph.karno", "sex")])

# 2. Dopasowanie modelu (sprawdź nazwę: out vs outs)
out <- prop.odds(Event(time, status == 2) ~ age + ph.ecog + ph.karno + sex,
                 data = lung_clean, n.sim = 0)

# 3. Wyświetlenie wyników
# Aby na pewno coś zobaczyć w R Markdown, używamy print()
print("--- Współczynniki modelu (Gamma) ---")
print(out$gamma)

print("--- Pełne podsumowanie ---")
print(summary(out))
```


```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
